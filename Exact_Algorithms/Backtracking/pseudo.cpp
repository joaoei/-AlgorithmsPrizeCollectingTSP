#include <iostream>
#include <vector>
#include <chrono>

std::vector<int> alg_backtracking(
	std::vector<int> prizes, 
	std::vector<int> penaltys, 
	std::vector<std::vector<int>> travel_cost,
	double prize_min) {

	//return result;
}

int main (int argc, char *argv[]) {
	//Receber parametros
	//Ler dados do arquivo
	//Determinar prize_min = Somatório dos bonus * alfa
	//Executar algoritmo
	//Determinar tempo gasto
	//Exibir resultado (Arestas, o bonus e a penalidade)

 	return 0;
}